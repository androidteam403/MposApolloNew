<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black"
        android:gravity="center"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/actionbar_color"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/header_back"
                android:layout_width="@dimen/dimen20dp"
                android:layout_height="@dimen/dimen20dp"
                android:contentDescription="@string/app_name"
                android:onClick="@{()-> mCallback.onClickBackIcon()}"
                android:padding="@dimen/dimen3dp"
                android:src="@drawable/ic_back_arrow_white" />

            <TextView
                android:id="@+id/header_name_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/ten_dp"
                android:fontFamily="@font/roboto_bold"
                android:text="@{orderPrescriptionURL.prescriptionno}"
                android:textColor="@color/white"
                android:textSize="@dimen/dimen14sp" />

        </LinearLayout>

        <com.apollopharmacy.mpospharmacistTest.utils.zoomage.ZoomageView
            android:id="@+id/prescription_preview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/app_name" />

    </LinearLayout>

    <data>

        <variable
            name="orderPrescriptionURL"
            type="com.apollopharmacy.mpospharmacistTest.ui.pbas.openorders.modelclass.GetOMSTransactionResponse.OrderPrescriptionURL" />

        <variable
            name="mCallback"
            type="com.apollopharmacy.mpospharmacistTest.ui.pbas.prescriptionslider.PrescriptionSliderMvpView" />
    </data>
</layout>