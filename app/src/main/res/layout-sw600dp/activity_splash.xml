<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.common.splash.SplashActivity">

        <ImageView
            android:id="@+id/image_app_logo"
            android:layout_width="@dimen/one_hundred_seventy_dp"
            android:layout_height="@dimen/one_hundred_seventy_dp"
            android:layout_centerInParent="true"
            android:background="@drawable/app_shadow_logo"
            android:contentDescription="@string/app_name" />

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="@dimen/twenty_dp"/>
    </RelativeLayout>

    <data>

        <variable
            name="viewModel"
            type="com.apollopharmacy.mpospharmacistTest.ui.splash.SplashMvpPresenter" />
    </data>
</layout>