<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/thirty_dp" />
    <solid android:color="@color/apollo_btn_color"/>
    <padding
        android:bottom="@dimen/five_dp"
        android:left="@dimen/five_dp"
        android:right="@dimen/five_dp"
        android:top="@dimen/five_dp" />
    <stroke
        android:width="@dimen/one_dp"
        android:color="@color/calculated_points_bg" />
</shape>